// Define a type for the category objects.
interface Category {
  value: string;
  label: string;
}

// Create an array of categories with the specified type.
const categories: Category[] = [
  { value: "English", label: "en" },
];

export default categories;
